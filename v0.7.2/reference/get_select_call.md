# Parse `dplyr` select call

Parse `dplyr` select call

## Usage

``` r
get_select_call(select)
```

## Arguments

- select:

  (`character`) vector of selected column names.

## Value

`dplyr` select `call`.
