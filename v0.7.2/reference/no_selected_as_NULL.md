# Check select choices for no choice made

Check select choices for no choice made

## Usage

``` r
no_selected_as_NULL(x)
```

## Arguments

- x:

  (`character`) Word that shall be checked for `NULL`, empty,
  "–no-selection".

## Value

The word or `NULL`.
