# Ensures datasets is a list of reactive expression

Ensures datasets is a list of reactive expression

## Usage

``` r
convert_teal_data(datasets)
```

## Arguments

- datasets:

  (`reactive` or `teal_data` or `list`) of `data.frame` wrapped or not
  in a reactive expression.

## Value

List of `reactive` expressions that contains all the individual
`datasets`.
