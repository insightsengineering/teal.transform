# Check data extract specification

Check data extract specification

## Usage

``` r
check_data_extract_spec(data_extract_spec)
```

## Arguments

- data_extract_spec:

  (`list`) of `data_extract_spec`.

## Value

Raises an error when check fails, otherwise, it returns the
`data_extract_spec` parameter, invisibly and unchanged.
