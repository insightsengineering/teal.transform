# Check selector internal_id element

Check selector internal_id element

## Usage

``` r
check_selector_internal_id(internal_id)
```

## Arguments

- internal_id:

  (`character(1)`) selector element generated by `data_extract_srv`.

## Value

Raises an error when check fails, otherwise, it returns the
`internal_id` parameter, invisibly and unchanged.
